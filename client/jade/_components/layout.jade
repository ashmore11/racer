template(name="layout")

	if currentUser

		+header

	.background

	main#main

		+transition
			+yield


template( name="transition" )
	+UI.contentBlock

template( name="header" )

	.back
		.arrow
			.line
			.line

	.nav_cta( class="#{active}" )
		.line
		.line
		.line

	header#nav( class="#{active}" )

		ul
			li.link: a( href="/" ) HOME
			li.link: a( href="/races" ) FIND A RACE